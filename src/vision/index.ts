/**
 * Vision System — Screen Capture
 *
 * Handles screenshot capture and change detection via the Tauri backend.
 * Screenshots are sent directly to cloud LLM providers for understanding.
 */

export * from './types';
export * from './capture';
